{
  "name": "boot",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start-app": "webpack-dev-server --env pfen=local",
    "dev-build": "webpack --mode=production --env pfen=dev",
    "compile-dev": "webpack --mode=production --env pfen=dev --watch",
    "prod-build": "webpack --mode=production --env pfen=prod && firebase deploy -P prod"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  }
}
